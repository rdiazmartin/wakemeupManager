---
scope: wakemeupManager completo — BE Python + app Android
purpose: consistency contract para builds independientes
altitude: feature (mantiene epics)
updated: 2026-09-13T16:22
---

- (decision) Paradigma: hexagonal/layered BE (adapters FastAPI/SQLite/SSH/network en núcleo de servicios), MVVM+UDF en Android. Límite: BE no depende de la app (API es contrato)
- (version) Stack BE: Python 3.14, FastAPI 0.141, aiosqlite 0.22, asyncssh 2.24; scan: binario ping del distro + proc/net/arp (no-root)
- (version) Stack Android: Kotlin 2.4.20, Compose BOM 2026.09.00 (M3 1.4.0), AGP 9.4/Gradle 9.6, targetSdk 36, Ktor 3.5.2, security-crypto 1.1 (Keystore+Encrypted DataStore 1.2.1), ViewModel+StateFlow UDF
- (decision) ssh-copy-id sin sshpass: asyncssh SFTP append authorized_keys + ejecución de shutdown; password por fichero tmp 600 O_EXCL no persistida
- (decision) ADOPTED: escucha solo tailnet+loopback (FR-9/10); token por dispositivo; fingerprint pinning; singleflight scans; SQLite persistencia
- (decision) Identidad máquina: (id interno, fingerprint host fijado); MAC del alta para WOL; IP solo referencia efímera de estado
- (question) OQ-A: ¿proceso BE systemd user o systemd system?; OQ-B: ¿uv como gestor de deps?
- (event) Inicio run: verificación web de stack completada
- (decision) AD-1 API como único contrato BE↔app
- (decision) AD-2 identidad = id+fingerprint; IP efímera; MAC para WOL
- (decision) AD-3 singleflight, TTL 60s/2, sin borrado salvo DELETE, autoexclusión
- (decision) AD-4 wake/shutdown solo vía servicios; shutdown verifica fingerprint; comando fijo allowlist; WOL Ethernet
- (decision) AD-5 alta con asyncssh en proceso (sin sshpass); password solo en memoria; SFTP append idempotente
- (decision) AD-6 solo tailnet+loopback; token hash SHA-256; backoff 5/5min->429/15min
- (decision) AD-7 SQLite único; claves fuera de DB (600/700); backup simple v1
- (decision) AD-8 UDF+StateFlow; polling 30s; token Keystore+Encrypted DataStore; strings ES/EN
- (assumption) minSdk 26; Ktor sobre Retrofit (alternativa aceptada); systemd system + usuario wakemeup; backups cron manual
- (event) Reviewer gate (rubric + tech + adversarial): 1 critical + 6 high, resueltos en spine
- (decision) AD-9 identidad SSH unificada (par Ed25519 despliegue, remote_user+fingerprint por máquina, comando en config)
- (decision) AD-10 DTO de máquina con status {online,offline,no_fiable} + managed; UI solo informativo en no_fiable
- (decision) Rutas API verbatim en AD-1 bajo /api/v1; /scan con escaneo en curso responde 202 (no 409); token hash pre-imagen hex-lower; contadores 429 en memoria
- (event) Spine finalized: 10 ADs, entornos operacional completo, stack verificado
- (change) Update MCP+SSE desde PRD r3: BE expone MCP (tools lectura+wake+shutdown+scan, sin alta, token dedicado) y emite eventos SSE a la app (FR-16..18, FR-10b, FR-11 canal)
- (decision) AD-11 bus asyncio pub/sub + SSE en /api/v1/events con Bearer token; Ktor SSE plugin; reconexión backoff; polling fallback; pausa en ahorro batería
- (decision) AD-12 MCP con SDK oficial (mcp 2.2.x) montado en FastAPI; token MCP en capa FastAPI; tools sin alta; mismo servicios de control
- (event) Spine update MCP+SSE: AD-11, AD-12 añadidos; AD-1/AD-8 ajustados; stack y mapa actualizados
- (version) mcp SDK 2.2.x (streamable_http_app montado en FastAPI, transport_security para tailnet); Ktor SSE integrado en ktor-client-core 3.5.2
