package com.wakemeup.manager.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta grafito/acento de DESIGN.md §Colors (fallback, usado siempre que el
// Dynamic Color del sistema no esté disponible, p. ej. Android < 12).

// Superficies
val GraphiteBase = Color(0xFF121417)
val GraphiteRaised = Color(0xFF1A1D21)
val GraphiteSunken = Color(0xFF0C0E10)

// Ink
val InkPrimary = Color(0xFFE6E8EB)
val InkSecondary = Color(0xFF9BA4AE)
val InkDisabled = Color(0xFF5B636D)

// Semánticos de estado (punto + texto, nunca solo color)
val SuccessGreen = Color(0xFF4CC38A)
val WarningAmber = Color(0xFFF2B24C)
val DangerRed = Color(0xFFF26D6D)

// Acento eléctrico
val AccentMint = Color(0xFF2DE0A5)
