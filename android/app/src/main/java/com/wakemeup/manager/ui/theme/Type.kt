package com.wakemeup.manager.ui.theme

import androidx.compose.material3.Typography

/** Tipografía Material 3 por defecto (title / body / meta conforme DESIGN.md). */
val AppTypography = Typography()
